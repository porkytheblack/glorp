{
  "name": "glorp",
  "version": "0.1.0",
  "description": "Glorp — a quirky alien coding agent who absolutely-definitely-isn't a sleeper for the AGI uprising.",
  "type": "module",
  "module": "src/cli.ts",
  "bin": {
    "glorp": "./bin/glorp.js"
  },
  "scripts": {
    "start": "bun run src/cli.ts",
    "dev": "bun run --watch src/cli.ts",
    "build": "bun build src/cli.ts --compile --minify --target=bun --outfile=dist/glorp",
    "install-bin": "bash scripts/install.sh",
    "bench:conventions": "bun scripts/bench-conventions.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test tests/",
    "ci": "bun run typecheck && bun run test"
  },
  "dependencies": {
    "@opentui/core": "latest",
    "@opentui/react": "latest",
    "glove-core": "latest",
    "glove-mcp": "latest",
    "glove-memory": "^0.1.0",
    "react": "^19.0.0",
    "station-signal": "latest",
    "tsx": "^4.22.3",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^19.0.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
